<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Weather App</title>
</head>

<body
    class="min-h-screen bg-gradient-to-br from-blue-500 via-indigo-600 to-purple-700 flex items-center justify-center px-4">

    <div
        class="w-full max-w-md bg-white/10 backdrop-blur-lg rounded-2xl shadow-2xl p-8 text-white border border-white/20">

        <!-- Title -->
        <h1 class="text-3xl font-bold text-center mb-2 tracking-wide">
            {{title}}
        </h1>

        <p class="text-center text-white/80 mb-6 text-sm">
            {{describ}}
        </p>

        <!-- Form -->
        <form action="" id="form" class="space-y-4">

            <div>
                <input id="inputf" type="text" placeholder="Enter country name..."
                    class="w-full px-4 py-3 rounded-xl bg-white/20 placeholder-white/70 text-white outline-none focus:ring-2 focus:ring-white focus:bg-white/30 transition duration-300">
            </div>

            <button id="btn1" type="submit"
                class="w-full bg-white text-indigo-700 font-semibold py-3 rounded-xl shadow-lg hover:scale-105 hover:bg-gray-100 transition duration-300">
                🔍 Search Weather
            </button>

        </form>
        <div id="txt-box"
            class="mt-8 w-full max-w-md mx-auto backdrop-blur-lg bg-white/10 border border-white/20 shadow-2xl rounded-2xl p-6 text-white transition-all duration-300 hover:scale-105 hover:shadow-indigo-500/40 hidden">

            <h2 class="text-2xl font-bold mb-6 text-center tracking-wide">
                🌍 Location Details
            </h2>

            <div class="flex flex-col gap-4 text-lg">

                <div class="flex justify-between items-center bg-white/5 px-4 py-3 rounded-xl">
                    <span class="font-semibold text-indigo-200 hover:text-yellow-400 transition-all duration-300">Country</span>
                    <span id="txtF1" class="font-bold text-white"></span>
                </div>

                <div class="flex justify-between items-center bg-white/5 px-4 py-3 rounded-xl">
                    <span class="font-semibold text-indigo-200 hover:text-yellow-400 transition-all duration-300">Latitude</span>
                    <span id="txtF2" class="font-bold text-white"></span>
                </div>

                <div class="flex justify-between items-center bg-white/5 px-4 py-3 rounded-xl">
                    <span class="font-semibold text-indigo-200 hover:text-yellow-400 transition-all duration-300">Longitude</span>
                    <span id="txtF3" class="font-bold text-white"></span>
                </div>

                <div class="flex justify-between items-center bg-white/5 px-4 py-3 rounded-xl">
                    <span class="font-semibold text-indigo-200 hover:text-yellow-400 transition-all duration-300">Temperature</span>
                    <span id="txtF4" class="font-bold text-yellow-300 text-xl"></span>
                </div>

            </div>

        </div>
        <p id="error" class="text-2xl mt-4 p-4 bg-red-600/50 rounded-2xl hidden"></p>

        <script src="/js/script.js"></script>
</body>

</html>